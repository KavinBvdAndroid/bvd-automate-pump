<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name=".core.MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LoginActivity"
        tools:targetApi="31">
        <activity
            android:name=".feature.automatefuel.SiteLocationListActivityCompose"
            android:exported="false"
            android:label="@string/title_activity_site_location_list_compose"
            android:theme="@style/Theme.LoginActivity" />
        <activity
            android:name=".feature.automatefuel.FetchingSiteLocationCompose"
            android:exported="false"
            android:label="@string/title_activity_fetching_site_location_compose"
            android:theme="@style/Theme.LoginActivity" />
        <activity
            android:name=".feature.auth.VinNumberActivityCompose"
            android:exported="false"
            android:label="@string/title_activity_vin_number_compose"
            android:theme="@style/Theme.LoginActivity" />
        <activity
            android:name=".feature.auth.LoginActivityCompose"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>